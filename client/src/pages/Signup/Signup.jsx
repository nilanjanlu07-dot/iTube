import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import GoogleAuthButton from '../../components/Auth/GoogleAuthButton'
function Signup() { const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const navigate = useNavigate(); const change = (e) => setForm({ ...form, [e.target.name]: e.target.value }); const submit = (e) => { e.preventDefault(); if (form.name.trim().length < 2) return setError('Enter a name with at least 2 characters.'); if (form.password.length < 6) return setError('Use a password with at least 6 characters.'); localStorage.setItem('itube-account', JSON.stringify(form)); localStorage.setItem('itube-user', JSON.stringify({ name: form.name, email: form.email })); navigate('/') }; const googleSuccess = (profile) => { localStorage.setItem('itube-user', JSON.stringify({ name: profile.name || profile.email.split('@')[0], email: profile.email, picture: profile.picture })); navigate('/') }; return <div className="grid min-h-screen place-items-center bg-gradient-to-br from-zinc-950 via-black to-zinc-900 px-5"><div className="w-full max-w-md rounded-3xl border border-white/10 bg-zinc-900 p-8 shadow-2xl"><Link to="/" className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-red-600 text-2xl">▶</Link><h1 className="mt-6 text-center text-3xl font-bold">Create your account</h1><p className="mt-2 text-center text-zinc-400">Join your video universe.</p><form onSubmit={submit} className="mt-8 space-y-4"><input name="name" value={form.name} onChange={change} required placeholder="Full name" className="w-full rounded-xl border border-zinc-700 bg-zinc-800 px-4 py-3 outline-none focus:border-red-500" /><input name="email" value={form.email} onChange={change} required type="email" placeholder="Email address" className="w-full rounded-xl border border-zinc-700 bg-zinc-800 px-4 py-3 outline-none focus:border-red-500" /><input name="password" value={form.password} onChange={change} required type="password" placeholder="Password (6+ characters)" className="w-full rounded-xl border border-zinc-700 bg-zinc-800 px-4 py-3 outline-none focus:border-red-500" />{error && <p className="text-sm text-red-400">{error}</p>}<button className="w-full rounded-xl bg-red-600 py-3 font-semibold hover:bg-red-700">Create account</button></form><div className="my-5 flex items-center gap-3 text-xs text-zinc-500"><span className="h-px flex-1 bg-zinc-700" />OR<span className="h-px flex-1 bg-zinc-700" /></div><GoogleAuthButton text="signup_with" onSuccess={googleSuccess} /><p className="mt-6 text-center text-sm text-zinc-400">Already have an account? <Link className="font-semibold text-red-400" to="/login">Login</Link></p></div></div> }
export default Signup
