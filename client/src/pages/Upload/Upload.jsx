import { useState } from 'react'
import { FiUploadCloud } from 'react-icons/fi'
function Upload() { const [file, setFile] = useState(null); const [done, setDone] = useState(false); const submit = (e) => { e.preventDefault(); if (file) setDone(true) }; return <div className="mx-auto max-w-2xl p-6"><h1 className="text-2xl font-bold">Upload video</h1>{done ? <div className="mt-8 rounded-xl border border-emerald-700 bg-emerald-950 p-6"><h2 className="text-lg font-bold">Upload complete</h2><p className="mt-2 text-zinc-300">Your video is processing and will appear on your channel shortly.</p></div> : <form onSubmit={submit} className="mt-8 rounded-2xl border border-zinc-800 bg-zinc-900 p-8 text-center"><FiUploadCloud className="mx-auto text-5xl text-zinc-400" /><h2 className="mt-4 text-lg font-semibold">Select video files to upload</h2><p className="mt-2 text-sm text-zinc-400">Your videos will be private until you publish them.</p><label className="mt-6 inline-block cursor-pointer rounded-full bg-white px-5 py-2.5 text-sm font-semibold text-black">Select files<input onChange={(e) => setFile(e.target.files?.[0])} className="hidden" type="file" accept="video/*" /></label>{file && <p className="mt-4 text-sm text-emerald-400">Ready to upload: {file.name}</p>}<button disabled={!file} className="ml-3 rounded-full bg-red-600 px-5 py-2.5 text-sm font-semibold disabled:opacity-40">Upload</button></form>}</div> }
export default Upload
